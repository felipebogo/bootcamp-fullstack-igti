import React from 'react';

/**
 * Componente utilizado para definir a posição
 * do candidato no card
 */
export default function CardPosition({ children }) {
  return <span style={{ fontWeight: 'bold' }}>{children}</span>;
}
